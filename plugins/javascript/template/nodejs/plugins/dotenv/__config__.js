export default {
    variables: {
        "index.js": {
            import: ["import * as dotenv from 'dotenv';", "dotenv.config();"],
        },
        "server.js": {
            import: ["import * as dotenv from 'dotenv';", "dotenv.config();"],
        },
    },
};
